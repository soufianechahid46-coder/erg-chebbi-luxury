export type GalleryItem = {
  src: string;
  alt: string;
};

export type Testimonial = {
  guest: string;
  country: string;
  quote: string;
  rating: number;
};

export type TrustPoint = {
  title: string;
  description: string;
};
