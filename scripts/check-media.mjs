const note = [
  'Media is configured in content/site/media.ts.',
  'Remote images require the domains declared in next.config.mjs.',
  'Fallback assets are stored in public/placeholders.'
];

console.log(note.join('\n'));
