# Vercel deployment

1. Import the GitHub repository into Vercel
2. Add the environment variables from `.env.example`
3. Deploy
4. Add your production URL to `NEXT_PUBLIC_SITE_URL`

The site works on preview URLs before a custom domain is connected.
