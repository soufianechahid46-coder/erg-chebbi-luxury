# Pricing updates

Edit the offer files in `content/site/offers/`.

Each offer contains:
- `fromPrice`
- `pricingModel`
- `duration`
- `includes`
- `addons`

Price logic is centralized in `lib/pricing.ts`.
