# Admin credentials

Update these environment variables:

- `ADMIN_USERNAME`
- `ADMIN_PASSWORD`
- `ADMIN_SESSION_SECRET`

The admin area uses a signed HTTP-only cookie created by `lib/admin-auth.ts`.
